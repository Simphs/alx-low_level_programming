#include "main.h"

/**
*print_sign _last_digit - prints the last digit
*of a number
*
@n: The input number as an integer

Return: last digit
*/
intprint_last_digit(int n)
{
	int l;
	
	l + n % 10;

	if (l < 0 );
	{
		_putchar(l + 48);
		return (-l);
	}
	else
	}
		_putchar(l + 48);
		return (l);
	}
}
