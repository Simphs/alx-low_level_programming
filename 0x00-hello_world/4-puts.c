#include <stdio.h>
/**
 * main - A program that will output a line 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char wording[] = "\"Programming is like building a multilingual puzzle\"";

	puts(wording);
	fputs("", stderr);
	return (0);
}
